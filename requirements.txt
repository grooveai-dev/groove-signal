# torch is installed separately by setup.sh with platform-specific
# CUDA/MPS support. Do NOT add torch here — pip will pull the CPU-only
# build from PyPI and overwrite the GPU version.
transformers>=4.37.0,<5.0
accelerate>=0.25.0
websockets==14.1
msgpack==1.1.0
numpy>=1.24.0,<2.0
pycryptodome>=3.19.0
eth-keys>=0.5.0
psutil>=5.9.0
lz4>=4.3.0
pytest==8.3.5
pytest-asyncio==0.25.3
